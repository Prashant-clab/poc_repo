name: Deploy Test

on:
  push:
    branches:
      - main
env:
  backend_value: 0
  frontend_value: 0

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: dorny/paths-filter@v2
        id: changes
        with:
          filters: |
            src:
              - '**'

      - if: steps.changes.output.src == 'true'
        run: |
          echo "HAHAHHAHAHA"
          
